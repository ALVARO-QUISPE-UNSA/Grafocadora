package grafos;
import java.util.*;
public class GrafoPonderado {
  private ArrayList<ArrayList <Integer> > grafo;
  private HashMap <String, Integer> vert;

  public void add (String v1, Integer w, String v2) {
    if (!vert.containsKey(v1)) vert.put(v1, vert.size());
    if (!vert.containsKey(v1)) vert.put(v1, vert.size());
  }
}
